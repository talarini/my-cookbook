<%= render "shared/header" %>
<p>Cadastro de Tipos de Receita</p>

<%= form_for(@recipe_type) do |f| %>

  <%= f.label :name, 'Nome' %>
  <%= f.text_field :name %>

  <%= f.submit 'Enviar'%>

  <% end %>
