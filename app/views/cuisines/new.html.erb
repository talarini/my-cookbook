<%= render "shared/header" %>
<p>Cadastro de Tipos de Cozinha</p>

<%= form_for(@cuisine) do |f| %>

  <%= f.label :name, 'Nome' %>
  <%= f.text_field :name %>

  <%= f.submit 'Enviar'%>

  <% end %>
